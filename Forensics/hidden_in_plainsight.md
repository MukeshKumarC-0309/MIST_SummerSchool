# Hidden in Plainsight


## Step-by-Step Solution
We open a file which has nothing in particular,just a normal image in plainsight.Then we use the 'file' command to see if there's anything hidden but there's nothing that seems to be out of the ordinary.Then we use a tool called exiftool to gather more information about the file.There we something peculiar under comments which seems to be base64 encoded.Decoding that gave us the flag.
